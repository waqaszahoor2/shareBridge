import { formatTransferCode, normalizeTransferCode } from '@/lib/utils/transferCode';

export { normalizeTransferCode, formatTransferCode };
export const formatTransferCodeDisplay = formatTransferCode;
